services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: hiri
      POSTGRES_PASSWORD: localdevpassword
      POSTGRES_DB: hiri
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
