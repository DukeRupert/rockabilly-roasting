package storefront

import "github.com/dukerupert/hiri/internal/ui/layouts"

type OrderConfirmedProps struct {
	OrderNumber string
	CartCount   int
}

templ OrderConfirmedContent(props OrderConfirmedProps) {
	<div class="mx-auto max-w-lg text-center py-12">
		<div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-rr-teal/15 mb-6">
			<svg class="h-8 w-8 text-rr-teal-lt" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
				<path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5"></path>
			</svg>
		</div>
		<h1 class="text-2xl font-bold text-rr-heading mb-2">Thank you for your order!</h1>
		<p class="text-rr-body mb-1">Your order has been confirmed.</p>
		<p class="text-lg font-semibold text-rr-red mb-8">
			Order { props.OrderNumber }
		</p>
		<p class="text-sm text-rr-muted mb-6">
			We'll send a confirmation email with your order details shortly.
		</p>
		<a
			href="/catalog"
			class="inline-block rounded-md bg-rr-red px-6 py-3 text-base font-semibold text-white shadow-sm hover:bg-rr-red-lt"
		>
			Continue shopping
		</a>
	</div>
}

templ OrderConfirmedPage(props OrderConfirmedProps) {
	@layouts.Storefront(layouts.StorefrontProps{
		Title:     "Order Confirmed",
		CartCount: props.CartCount,
	}) {
		@OrderConfirmedContent(props)
	}
}
