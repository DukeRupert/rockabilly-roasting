package storefront

import (
	"fmt"

	"github.com/dukerupert/hiri/internal/domain"
	"github.com/dukerupert/hiri/internal/ui/layouts"
)

// --- Shared wholesale account layout ---
//
// Mirrors the retail accountLayout but is scoped to the wholesale portal: the
// nav points at /wholesale/account/* (plus Quick Order → /wholesale/portal),
// sign-out posts to /wholesale/logout, and there's no email-verification
// banner — wholesale accounts authenticate with a password from setup. The
// class helpers (accountNavClass, mobileNavClass) and the accountPageHeading /
// orderStatusBadge / addressForm templates are shared with the retail account.

templ wholesaleAccountNav(activePath string) {
	<nav class="w-48 shrink-0" aria-label="Account sections">
		<p class="font-oswald font-bold text-candle text-[11px] mb-3 pb-2 border-b-2 border-ink" style="letter-spacing:0.24em; text-transform:uppercase;">The Crew</p>
		<div class="space-y-0.5">
			<a href="/wholesale/portal" class={ accountNavClass("/wholesale/portal", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 0 0 2.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 0 0-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 0 0 .75-.75 2.25 2.25 0 0 0-.1-.664m-5.8 0A2.251 2.251 0 0 1 13.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25Z"></path></svg>
				Quick Order
			</a>
			<a href="/wholesale/account/orders" class={ accountNavClass("/wholesale/account/orders", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z"></path></svg>
				Orders
			</a>
			<a href="/wholesale/account/settings" class={ accountNavClass("/wholesale/account/settings", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M9.594 3.94c.09-.542.56-.94 1.11-.94h2.593c.55 0 1.02.398 1.11.94l.213 1.281c.063.374.313.686.645.87.074.04.147.083.22.127.325.196.72.257 1.075.124l1.217-.456a1.125 1.125 0 011.37.49l1.296 2.247a1.125 1.125 0 01-.26 1.431l-1.003.827c-.293.241-.438.613-.43.992a7.723 7.723 0 010 .255c-.008.378.137.75.43.991l1.004.827c.424.35.534.955.26 1.43l-1.298 2.247a1.125 1.125 0 01-1.369.491l-1.217-.456c-.355-.133-.75-.072-1.076.124a6.47 6.47 0 01-.22.128c-.331.183-.581.495-.644.869l-.213 1.281c-.09.543-.56.941-1.11.941h-2.594c-.55 0-1.019-.398-1.11-.94l-.213-1.281c-.062-.374-.312-.686-.644-.87a6.52 6.52 0 01-.22-.127c-.325-.196-.72-.257-1.076-.124l-1.217.456a1.125 1.125 0 01-1.369-.49l-1.297-2.247a1.125 1.125 0 01.26-1.431l1.004-.827c.292-.24.437-.613.43-.991a6.932 6.932 0 010-.255c.007-.38-.138-.751-.43-.992l-1.004-.827a1.125 1.125 0 01-.26-1.43l1.297-2.247a1.125 1.125 0 011.37-.491l1.216.456c.356.133.751.072 1.076-.124.072-.044.146-.086.22-.128.332-.183.582-.495.644-.869l.214-1.28z"></path><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
				Settings
			</a>
			<a href="/wholesale/account/addresses" class={ accountNavClass("/wholesale/account/addresses", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z"></path></svg>
				Addresses
			</a>
			<a href="/wholesale/account/team" class={ accountNavClass("/wholesale/account/team", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19.128a9.38 9.38 0 0 0 2.625.372 9.337 9.337 0 0 0 4.121-.952 4.125 4.125 0 0 0-7.533-2.493M15 19.128v-.003c0-1.113-.285-2.16-.786-3.07M15 19.128v.106A12.318 12.318 0 0 1 8.624 21c-2.331 0-4.512-.645-6.374-1.766l-.001-.109a6.375 6.375 0 0 1 11.964-3.07M12 6.375a3.375 3.375 0 1 1-6.75 0 3.375 3.375 0 0 1 6.75 0Zm8.25 2.25a2.625 2.625 0 1 1-5.25 0 2.625 2.625 0 0 1 5.25 0Z"></path></svg>
				Team
			</a>
			<a href="/wholesale/account/security" class={ accountNavClass("/wholesale/account/security", activePath) } style="letter-spacing:0.04em;">
				<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z"/></svg>
				Security
			</a>
		</div>
		<div class="mt-4 pt-4 border-t-2 border-ink">
			<form method="POST" action="/wholesale/logout">
				<button type="submit" class="flex w-full items-center gap-3 px-3 py-2.5 font-oswald text-sm text-ink-soft hover:text-rust transition-colors" style="letter-spacing:0.04em;">
					<svg class="size-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0013.5 3h-6a2.25 2.25 0 00-2.25 2.25v13.5A2.25 2.25 0 007.5 21h6a2.25 2.25 0 002.25-2.25V15m3 0l3-3m0 0l-3-3m3 3H9"></path></svg>
					Sign out
				</button>
			</form>
		</div>
	</nav>
}

templ wholesaleAccountLayout(activePath, companyName string) {
	<!-- Full-bleed paper surface -->
	<section class="-mx-4 sm:-mx-6 lg:-mx-8 relative bg-paper min-h-[calc(100vh-4rem)]">
		<!-- Paper grain overlay -->
		<div class="absolute inset-0 opacity-[0.05] pointer-events-none" style="background-image: radial-gradient(circle, rgba(14,13,12,0.6) 1px, transparent 1px); background-size: 3px 3px;"></div>
		<div class="relative mx-auto max-w-6xl px-6 sm:px-10 lg:px-14 py-10 sm:py-14">
			<!-- Mobile nav (horizontal pills) -->
			<div class="sm:hidden mb-8">
				<div class="flex gap-2 overflow-x-auto pb-3 border-b-2 border-ink">
					<a href="/wholesale/portal" class={ mobileNavClass("/wholesale/portal", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Order</a>
					<a href="/wholesale/account/orders" class={ mobileNavClass("/wholesale/account/orders", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Orders</a>
					<a href="/wholesale/account/settings" class={ mobileNavClass("/wholesale/account/settings", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Settings</a>
					<a href="/wholesale/account/addresses" class={ mobileNavClass("/wholesale/account/addresses", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Addresses</a>
					<a href="/wholesale/account/team" class={ mobileNavClass("/wholesale/account/team", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Team</a>
					<a href="/wholesale/account/security" class={ mobileNavClass("/wholesale/account/security", activePath) } style="letter-spacing:0.14em; text-transform:uppercase;">Security</a>
					<form method="POST" action="/wholesale/logout" class="ml-auto shrink-0">
						<button type="submit" class="whitespace-nowrap font-oswald font-bold text-[11px] border-2 border-ink bg-paper text-ink px-3 py-1.5 hover:bg-ink hover:text-paper transition-colors" style="letter-spacing:0.14em; text-transform:uppercase;">
							Sign out
						</button>
					</form>
				</div>
			</div>
			<!-- Desktop: sidebar + content; Mobile: content only -->
			<div class="sm:flex sm:gap-10">
				<div class="hidden sm:block sm:sticky sm:top-24 sm:self-start">
					@wholesaleAccountNav(activePath)
					if companyName != "" {
						<p class="mt-5 px-3 font-special text-chrome-deep text-xs leading-snug">Signed in as<br/><span class="text-ink">{ companyName }</span></p>
					}
				</div>
				<div class="flex-1 min-w-0">
					{ children... }
				</div>
			</div>
		</div>
	</section>
}

// --- Orders ---

type WholesaleAccountOrdersProps struct {
	Customer    *domain.Customer
	CompanyName string
	Orders      []domain.Order
	CartCount   int
}

templ WholesaleAccountOrdersContent(props WholesaleAccountOrdersProps) {
	@wholesaleAccountLayout("/wholesale/account/orders", props.CompanyName) {
		@accountPageHeading("The Crew · Wholesale", "Order history")
		if len(props.Orders) == 0 {
			<div class="border-2 border-ink bg-cream-hi py-14 px-6 text-center shadow-stamp">
				<svg class="mx-auto size-10 text-ink mb-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 10.5V6a3.75 3.75 0 10-7.5 0v4.5m11.356-1.993l1.263 12c.07.665-.45 1.243-1.119 1.243H4.25a1.125 1.125 0 01-1.12-1.243l1.264-12A1.125 1.125 0 015.513 7.5h12.974c.576 0 1.059.435 1.119 1.007zM8.625 10.5a.375.375 0 11-.75 0 .375.375 0 01.75 0zm7.5 0a.375.375 0 11-.75 0 .375.375 0 01.75 0z"></path></svg>
				<p class="font-slab text-ink text-lg uppercase" style="letter-spacing:0.02em;">No orders yet</p>
				<p class="font-oswald text-ink-soft text-sm mt-1" style="letter-spacing:0.04em;">Your placed orders will show up here.</p>
				<a href="/wholesale/portal" class="btn-stamp inline-flex items-center gap-2 mt-6 bg-rust text-paper border-2 border-ink px-6 py-3 font-oswald font-bold text-sm" style="letter-spacing:0.14em; text-transform:uppercase;">
					Start an order
					<svg class="size-4" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" aria-hidden="true">
						<path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3"/>
					</svg>
				</a>
			</div>
		} else {
			<div class="space-y-3">
				for _, order := range props.Orders {
					<!--
						Stretched-link card: the whole card navigates to the order
						(via the absolute <a>), while the Reorder form sits above it
						(pointer-events-auto + z-10) as a working, separate action —
						a button can't legally nest inside the navigating anchor.
					-->
					<div class="btn-stamp group relative border-2 border-ink bg-cream-hi p-4 sm:p-5">
						<a href={ templ.SafeURL("/wholesale/account/orders/" + order.ID.String()) } class="absolute inset-0" aria-label={ "View order " + order.Number }></a>
						<div class="relative pointer-events-none flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
							<div class="flex flex-col sm:flex-row sm:items-baseline gap-2 sm:gap-4 min-w-0">
								<span class="font-slab text-ink uppercase text-base sm:text-lg group-hover:text-rust transition-colors" style="letter-spacing:-0.005em;">{ order.Number }</span>
								<span class="font-special text-chrome-deep text-sm">{ formatDate(order.PlacedAt) }</span>
							</div>
							<div class="flex items-center gap-3 sm:gap-4">
								<span class="font-special text-ink text-base">{ formatCents(order.Total) }</span>
								@orderStatusBadge(order.Status, order.FulfillmentStatus)
								<form method="POST" action={ templ.SafeURL("/wholesale/account/orders/" + order.ID.String() + "/reorder") } class="pointer-events-auto relative z-10 shrink-0">
									<button type="submit" class="inline-flex items-center gap-1.5 border-2 border-ink bg-paper text-ink px-3 py-1.5 font-oswald font-bold text-[11px] hover:bg-rust hover:text-paper transition-colors" style="letter-spacing:0.14em; text-transform:uppercase;">
										<svg class="size-3.5" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" aria-hidden="true">
											<path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0 3.181 3.183a8.25 8.25 0 0 0 13.803-3.7M4.031 9.865a8.25 8.25 0 0 1 13.803-3.7l3.181 3.182m0-4.991v4.99"></path>
										</svg>
										Reorder
									</button>
								</form>
							</div>
						</div>
					</div>
				}
			</div>
		}
	}
}

templ WholesaleAccountOrdersPage(props WholesaleAccountOrdersProps) {
	@layouts.Storefront(layouts.StorefrontProps{Title: "Order History", CartURL: "/wholesale/checkout", CartCount: props.CartCount}) {
		@WholesaleAccountOrdersContent(props)
	}
}

// --- Order Detail ---

type WholesaleAccountOrderShowProps struct {
	Customer    *domain.Customer
	CompanyName string
	Order       *domain.Order
	LineItems   []OrderLineItemView
	Shipments   []domain.Shipment
	Address     *domain.Address
	CartCount   int
}

templ WholesaleAccountOrderShowContent(props WholesaleAccountOrderShowProps) {
	@wholesaleAccountLayout("/wholesale/account/orders", props.CompanyName) {
		<div>
			<div class="mb-6">
				<a href="/wholesale/account/orders" class="inline-flex items-center gap-1.5 font-oswald font-bold text-[11px] text-chrome-deep hover:text-rust transition-colors" style="letter-spacing:0.2em; text-transform:uppercase;">
					<svg class="size-3" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" aria-hidden="true">
						<path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18"/>
					</svg>
					Back to orders
				</a>
			</div>
			<div class="flex flex-wrap items-baseline gap-3 sm:gap-4 mb-8">
				<h1 class="font-slab text-ink uppercase leading-[0.92]" style="font-size: clamp(1.75rem, 4vw, 2.5rem); letter-spacing:-0.005em;">{ props.Order.Number }</h1>
				<span class="font-special text-chrome-deep text-base">{ formatDate(props.Order.PlacedAt) }</span>
				@orderStatusBadge(props.Order.Status, props.Order.FulfillmentStatus)
				<form method="POST" action={ templ.SafeURL("/wholesale/account/orders/" + props.Order.ID.String() + "/reorder") } class="ml-auto self-center">
					<button type="submit" class="btn-stamp inline-flex items-center gap-2 bg-rust text-paper border-2 border-ink px-5 py-2.5 font-oswald font-bold text-sm" style="letter-spacing:0.16em; text-transform:uppercase;">
						<svg class="size-4" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" aria-hidden="true">
							<path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0 3.181 3.183a8.25 8.25 0 0 0 13.803-3.7M4.031 9.865a8.25 8.25 0 0 1 13.803-3.7l3.181 3.182m0-4.991v4.99"></path>
						</svg>
						Reorder
					</button>
				</form>
			</div>
			<!-- Line items — receipt treatment -->
			<div class="border-2 border-ink bg-cream-hi overflow-hidden mb-8 shadow-stamp">
				<div class="border-b-2 border-ink px-4 sm:px-5 py-2.5 flex items-center justify-between bg-paper-warm">
					<span class="font-oswald font-bold text-ink text-[11px]" style="letter-spacing:0.2em; text-transform:uppercase;">Items</span>
					<span class="font-special text-chrome-deep text-xs">receipt #{ props.Order.Number }</span>
				</div>
				<table class="min-w-full">
					<thead class="border-b-2 border-ink">
						<tr>
							<th class="px-4 sm:px-5 py-2.5 text-left font-oswald font-bold text-ink text-[11px]" style="letter-spacing:0.2em; text-transform:uppercase;">Item</th>
							<th class="px-3 py-2.5 text-right font-oswald font-bold text-ink text-[11px]" style="letter-spacing:0.2em; text-transform:uppercase;">Qty</th>
							<th class="px-3 py-2.5 text-right font-oswald font-bold text-ink text-[11px]" style="letter-spacing:0.2em; text-transform:uppercase;">Price</th>
							<th class="px-4 sm:px-5 py-2.5 text-right font-oswald font-bold text-ink text-[11px]" style="letter-spacing:0.2em; text-transform:uppercase;">Total</th>
						</tr>
					</thead>
					<tbody class="divide-y-2 divide-ink/30">
						for _, item := range props.LineItems {
							<tr>
								<td class="px-4 sm:px-5 py-3">
									<span class="block font-oswald font-bold text-ink text-sm" style="letter-spacing:0.02em;">{ item.ProductName }</span>
									if item.VariantLabel != "" {
										<span class="block font-oswald text-chrome-deep text-xs" style="letter-spacing:0.04em;">{ item.VariantLabel }</span>
									}
								</td>
								<td class="px-3 py-3 font-special text-ink text-sm text-right">{ fmt.Sprintf("%d", item.Quantity) }</td>
								<td class="px-3 py-3 font-special text-ink-soft text-sm text-right">{ formatCents(item.UnitPrice) }</td>
								<td class="px-4 sm:px-5 py-3 font-special text-ink text-sm text-right">{ formatCents(item.Total) }</td>
							</tr>
						}
					</tbody>
					<tfoot class="border-t-2 border-ink bg-paper-warm">
						<tr>
							<td colspan="3" class="px-4 sm:px-5 py-2 font-oswald text-ink-soft text-sm text-right" style="letter-spacing:0.04em;">Subtotal</td>
							<td class="px-4 sm:px-5 py-2 font-special text-ink text-sm text-right">{ formatCents(props.Order.Subtotal) }</td>
						</tr>
						if props.Order.ShippingTotal > 0 {
							<tr>
								<td colspan="3" class="px-4 sm:px-5 py-2 font-oswald text-ink-soft text-sm text-right" style="letter-spacing:0.04em;">Shipping</td>
								<td class="px-4 sm:px-5 py-2 font-special text-ink text-sm text-right">{ formatCents(props.Order.ShippingTotal) }</td>
							</tr>
						}
						if props.Order.TaxTotal > 0 {
							<tr>
								<td colspan="3" class="px-4 sm:px-5 py-2 font-oswald text-ink-soft text-sm text-right" style="letter-spacing:0.04em;">Tax</td>
								<td class="px-4 sm:px-5 py-2 font-special text-ink text-sm text-right">{ formatCents(props.Order.TaxTotal) }</td>
							</tr>
						}
						<tr class="border-t-2 border-ink">
							<td colspan="3" class="px-4 sm:px-5 py-3 font-slab text-ink uppercase text-base text-right" style="letter-spacing:0.02em;">Total</td>
							<td class="px-4 sm:px-5 py-3 font-special text-ink text-lg text-right">{ formatCents(props.Order.Total) }</td>
						</tr>
					</tfoot>
				</table>
			</div>
			<div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
				<!-- Shipping address -->
				if props.Address != nil {
					<div class="border-2 border-ink bg-cream-hi p-5">
						<p class="font-oswald font-bold text-candle text-[11px] mb-3 pb-2 border-b-2 border-ink" style="letter-spacing:0.24em; text-transform:uppercase;">Shipped to</p>
						<p class="font-oswald text-ink-soft text-sm leading-relaxed" style="letter-spacing:0.04em;">
							{ props.Address.FirstName } { props.Address.LastName }<br/>
							if props.Address.Company != nil {
								{ *props.Address.Company }<br/>
							}
							{ props.Address.Line1 }<br/>
							if props.Address.Line2 != nil {
								{ *props.Address.Line2 }<br/>
							}
							{ props.Address.City }, { props.Address.State } { props.Address.PostalCode }
						</p>
					</div>
				}
				<!-- Tracking -->
				if len(props.Shipments) > 0 {
					<div class="border-2 border-ink bg-cream-hi p-5">
						<p class="font-oswald font-bold text-candle text-[11px] mb-3 pb-2 border-b-2 border-ink" style="letter-spacing:0.24em; text-transform:uppercase;">Tracking</p>
						<div class="space-y-2">
							for _, s := range props.Shipments {
								<div>
									<p class="font-oswald font-bold text-ink text-sm" style="letter-spacing:0.04em;">{ s.CarrierName }</p>
									<p class="font-special text-ink-soft text-xs">{ s.TrackingNumber }</p>
								</div>
							}
						</div>
					</div>
				}
			</div>
		</div>
	}
}

templ WholesaleAccountOrderShowPage(props WholesaleAccountOrderShowProps) {
	@layouts.Storefront(layouts.StorefrontProps{Title: "Order " + props.Order.Number, CartURL: "/wholesale/checkout", CartCount: props.CartCount}) {
		@WholesaleAccountOrderShowContent(props)
	}
}

// --- Settings ---

type WholesaleAccountSettingsProps struct {
	Customer    *domain.Customer
	CompanyName string
	CartCount   int
	Success     string
	Error       string
}

templ WholesaleAccountSettingsContent(props WholesaleAccountSettingsProps) {
	@wholesaleAccountLayout("/wholesale/account/settings", props.CompanyName) {
		<div class="max-w-lg">
			@accountPageHeading("The Crew · Wholesale", "Your details")
			if props.Success != "" {
				<div class="mb-6 border-2 border-ink bg-candle/20 px-4 py-3">
					<p class="font-oswald text-ink text-sm" style="letter-spacing:0.04em;">{ props.Success }</p>
				</div>
			}
			if props.Error != "" {
				<div class="mb-6 border-2 border-rust bg-cream-hi px-4 py-3">
					<p class="font-oswald font-bold text-rust text-sm" style="letter-spacing:0.04em;">{ props.Error }</p>
				</div>
			}
			<form method="POST" action="/wholesale/account/settings" class="space-y-5">
				<div class="grid grid-cols-2 gap-4">
					<div>
						<label for="first_name" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">First name</label>
						<input type="text" id="first_name" name="first_name" value={ props.Customer.FirstName } required
							class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink focus:outline-none" style="letter-spacing:0.04em;"/>
					</div>
					<div>
						<label for="last_name" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">Last name</label>
						<input type="text" id="last_name" name="last_name" value={ props.Customer.LastName } required
							class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink focus:outline-none" style="letter-spacing:0.04em;"/>
					</div>
				</div>
				<div>
					<label for="phone" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">Phone (optional)</label>
					<input type="tel" id="phone" name="phone" value={ phoneValue(props.Customer.Phone) } autocomplete="tel" placeholder="(509) 555-0123"
						class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink placeholder:text-chrome-deep focus:outline-none" style="letter-spacing:0.04em;"/>
					<p class="mt-2 font-oswald text-chrome-deep text-xs" style="letter-spacing:0.04em;">We'll only use this for order updates. Leave blank to remove.</p>
				</div>
				<div class="pt-2">
					<button type="submit" class="btn-stamp inline-flex items-center gap-2 bg-rust text-paper border-2 border-ink px-6 py-3 font-oswald font-bold text-sm" style="letter-spacing:0.16em; text-transform:uppercase;">
						Save changes
					</button>
				</div>
			</form>
			<!-- Account details on file — managed by the shop -->
			<div class="mt-10 pt-6 border-t-2 border-ink/30 space-y-4">
				<p class="font-oswald font-bold text-candle text-[11px]" style="letter-spacing:0.24em; text-transform:uppercase;">On file</p>
				<div>
					<p class="font-oswald font-bold text-ink text-[11px] mb-1" style="letter-spacing:0.2em; text-transform:uppercase;">Company</p>
					<p class="font-oswald text-ink-soft text-sm" style="letter-spacing:0.04em;">
						if props.CompanyName != "" {
							{ props.CompanyName }
						} else {
							—
						}
					</p>
				</div>
				<div>
					<p class="font-oswald font-bold text-ink text-[11px] mb-1" style="letter-spacing:0.2em; text-transform:uppercase;">Sign-in email</p>
					<p class="font-oswald text-ink-soft text-sm" style="letter-spacing:0.04em;">{ props.Customer.Email }</p>
				</div>
				<p class="font-oswald text-chrome-deep text-xs" style="letter-spacing:0.04em;">Need to change your company name or email? Drop the crew a line — we'll sort it.</p>
			</div>
		</div>
	}
}

templ WholesaleAccountSettingsPage(props WholesaleAccountSettingsProps) {
	@layouts.Storefront(layouts.StorefrontProps{Title: "Account Settings", CartURL: "/wholesale/checkout", CartCount: props.CartCount}) {
		@WholesaleAccountSettingsContent(props)
	}
}

// --- Addresses ---

type WholesaleAccountAddressesProps struct {
	Customer    *domain.Customer
	CompanyName string
	Addresses   []domain.Address
	CartCount   int
}

templ WholesaleAccountAddressesContent(props WholesaleAccountAddressesProps) {
	@wholesaleAccountLayout("/wholesale/account/addresses", props.CompanyName) {
		<div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-8">
			<div>
				<p class="font-oswald text-chrome-deep text-xs font-semibold" style="letter-spacing:0.24em; text-transform:uppercase;">
					Ship-to book
				</p>
				<h1 class="font-slab text-ink uppercase leading-[0.92] mt-2" style="font-size: clamp(1.75rem, 4vw, 2.5rem); letter-spacing:-0.005em;">
					Your addresses
				</h1>
			</div>
			<button type="button" x-data x-on:click="$dispatch('toggle-add-address')"
				class="btn-stamp inline-flex items-center gap-2 bg-rust text-paper border-2 border-ink px-5 py-2.5 font-oswald font-bold text-[11px] self-start sm:self-auto"
				style="letter-spacing:0.16em; text-transform:uppercase;">
				<svg class="size-3.5" fill="none" viewBox="0 0 24 24" stroke-width="3" stroke="currentColor" aria-hidden="true">
					<path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15"/>
				</svg>
				Add address
			</button>
		</div>
		<!-- Add address form (hidden by default) -->
		<div x-data="{ open: false }" x-on:toggle-add-address.window="open = !open">
			<div x-show="open" x-cloak class="mb-6 border-2 border-ink bg-cream-hi p-5 sm:p-6 shadow-stamp">
				<p class="font-oswald font-bold text-candle text-[11px] mb-4 pb-2 border-b-2 border-ink" style="letter-spacing:0.24em; text-transform:uppercase;">New address</p>
				@addressForm("/wholesale/account/addresses", nil)
			</div>
		</div>
		if len(props.Addresses) == 0 {
			<div class="border-2 border-ink bg-cream-hi py-12 px-6 text-center">
				<p class="font-oswald text-ink text-base" style="letter-spacing:0.04em;">No saved addresses yet.</p>
				<p class="font-oswald text-chrome-deep text-sm mt-2" style="letter-spacing:0.04em;">Add one above to speed up checkout.</p>
			</div>
		} else {
			<div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-5">
				for _, addr := range props.Addresses {
					<div class="border-2 border-ink bg-cream-hi shadow-stamp p-5" x-data="{ editing: false }">
						<div x-show="!editing">
							<div class="flex items-start justify-between gap-3 mb-3">
								<p class="font-slab text-ink uppercase text-base leading-[1.0]" style="letter-spacing:-0.005em;">{ addr.FirstName } { addr.LastName }</p>
								if addr.IsDefault {
									<span class="inline-flex items-center font-oswald font-bold text-[10px] text-ink bg-candle border-2 border-ink px-2 py-0.5 shrink-0" style="letter-spacing:0.16em; text-transform:uppercase;">
										Default
									</span>
								}
							</div>
							<p class="font-oswald text-ink-soft text-sm leading-relaxed" style="letter-spacing:0.04em;">
								if addr.Company != nil {
									{ *addr.Company }<br/>
								}
								{ addr.Line1 }<br/>
								if addr.Line2 != nil {
									{ *addr.Line2 }<br/>
								}
								{ addr.City }, { addr.State } { addr.PostalCode }
							</p>
							<div class="mt-4 pt-3 border-t border-ink flex flex-wrap gap-4">
								<button x-on:click="editing = true" class="font-oswald font-bold text-[11px] text-rust hover:text-rust-deep transition-colors" style="letter-spacing:0.18em; text-transform:uppercase;">Edit</button>
								if !addr.IsDefault {
									<form method="POST" action={ templ.SafeURL("/wholesale/account/addresses/" + addr.ID.String() + "/default") }>
										<button type="submit" class="font-oswald font-bold text-[11px] text-ink hover:text-rust transition-colors" style="letter-spacing:0.18em; text-transform:uppercase;">Set default</button>
									</form>
									<form method="POST" action={ templ.SafeURL("/wholesale/account/addresses/" + addr.ID.String() + "/delete") }
										x-data x-on:submit="if (!confirm('Delete this address?')) $event.preventDefault()">
										<button type="submit" class="font-oswald font-bold text-[11px] text-chrome-deep hover:text-rust transition-colors" style="letter-spacing:0.18em; text-transform:uppercase;">Delete</button>
									</form>
								}
							</div>
						</div>
						<div x-show="editing" x-cloak>
							<p class="font-oswald font-bold text-candle text-[11px] mb-4 pb-2 border-b-2 border-ink" style="letter-spacing:0.24em; text-transform:uppercase;">Edit address</p>
							@addressForm("/wholesale/account/addresses/"+addr.ID.String(), &addr)
							<button x-on:click="editing = false" class="mt-3 font-oswald font-bold text-[11px] text-chrome-deep hover:text-ink transition-colors" style="letter-spacing:0.18em; text-transform:uppercase;">Cancel</button>
						</div>
					</div>
				}
			</div>
		}
	}
}

templ WholesaleAccountAddressesPage(props WholesaleAccountAddressesProps) {
	@layouts.Storefront(layouts.StorefrontProps{Title: "Addresses", CartURL: "/wholesale/checkout", CartCount: props.CartCount}) {
		@WholesaleAccountAddressesContent(props)
	}
}

// --- Security ---

type WholesaleAccountSecurityProps struct {
	Customer    *domain.Customer
	CompanyName string
	HasPassword bool
	CartCount   int
	Error       string
	Success     string
}

templ WholesaleAccountSecurityContent(props WholesaleAccountSecurityProps) {
	@wholesaleAccountLayout("/wholesale/account/security", props.CompanyName) {
		<div class="max-w-lg">
			@accountPageHeading("The Crew · Wholesale", "Password")
			if props.Error != "" {
				<div class="mb-6 border-2 border-rust bg-cream-hi px-4 py-3">
					<p class="font-oswald font-bold text-rust text-sm" style="letter-spacing:0.04em;">{ props.Error }</p>
				</div>
			}
			if props.Success != "" {
				<div class="mb-6 border-2 border-ink bg-candle/20 px-4 py-3">
					<p class="font-oswald text-ink text-sm" style="letter-spacing:0.04em;">{ props.Success }</p>
				</div>
			}
			if !props.HasPassword {
				<!-- Set password for the first time -->
				<p class="mb-6 font-oswald text-ink-soft text-sm" style="letter-spacing:0.04em;">
					Add a password to your account so you can sign in to the wholesale portal.
				</p>
				<form method="POST" action="/wholesale/account/security/set" class="space-y-5">
					<div>
						<label for="new_password" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">New password</label>
						<input type="password" id="new_password" name="new_password" required minlength="10" autocomplete="new-password"
							class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink focus:outline-none" style="letter-spacing:0.04em;"/>
						<p class="mt-2 font-oswald text-chrome-deep text-xs" style="letter-spacing:0.04em;">Minimum 10 characters.</p>
					</div>
					<div class="pt-2">
						<button type="submit" class="btn-stamp inline-flex items-center gap-2 bg-rust text-paper border-2 border-ink px-6 py-3 font-oswald font-bold text-sm" style="letter-spacing:0.16em; text-transform:uppercase;">
							Set password
						</button>
					</div>
				</form>
			} else {
				<!-- Change existing password -->
				<p class="mb-6 font-oswald text-ink-soft text-sm" style="letter-spacing:0.04em;">
					Update the password you use to sign in to the wholesale portal.
				</p>
				<form method="POST" action="/wholesale/account/security/change" class="space-y-5">
					<div>
						<label for="current_password" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">Current password</label>
						<input type="password" id="current_password" name="current_password" required autocomplete="current-password"
							class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink focus:outline-none" style="letter-spacing:0.04em;"/>
					</div>
					<div>
						<label for="new_password" class="font-oswald font-bold text-ink text-[11px] mb-2 block" style="letter-spacing:0.2em; text-transform:uppercase;">New password</label>
						<input type="password" id="new_password" name="new_password" required minlength="10" autocomplete="new-password"
							class="paper-input w-full border-2 border-ink bg-cream-hi px-3.5 py-2.5 font-oswald text-sm text-ink focus:outline-none" style="letter-spacing:0.04em;"/>
						<p class="mt-2 font-oswald text-chrome-deep text-xs" style="letter-spacing:0.04em;">Minimum 10 characters.</p>
					</div>
					<div class="pt-2">
						<button type="submit" class="btn-stamp inline-flex items-center gap-2 bg-rust text-paper border-2 border-ink px-6 py-3 font-oswald font-bold text-sm" style="letter-spacing:0.16em; text-transform:uppercase;">
							Update password
						</button>
					</div>
				</form>
			}
		</div>
	}
}

templ WholesaleAccountSecurityPage(props WholesaleAccountSecurityProps) {
	@layouts.Storefront(layouts.StorefrontProps{Title: "Password & Security", CartURL: "/wholesale/checkout", CartCount: props.CartCount}) {
		@WholesaleAccountSecurityContent(props)
	}
}
